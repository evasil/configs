 if changed checksum      for 5 cycles  then alert
 if failed permission 755 for 5 cycles then alert
 if failed uid root       for 5 cycles then alert
 if failed gid root       for 5 cycles then alert

 if failed checksum       for 3 cycles then alert
 if failed permission 600 for 3 cycles then alert
 if failed uid root       for 3 cycles then alert
 if failed gid root       for 3 cycles then alert
